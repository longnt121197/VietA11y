{
  "name": "vieta11y",
  "version": "0.1.0",
  "private": true,
  "description": "Vietnamese Web Accessibility Scanner",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/longnt121197/vieta11y.git"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run build --workspace @vieta11y/scanner && npm run dev --workspace @vieta11y/web",
    "build": "npm run build --workspace @vieta11y/scanner && npm run build --workspace @vieta11y/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build --workspace @vieta11y/scanner && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0"
  }
}
